cmake_minimum_required(VERSION 3.16.3)

# set the project name
project (project_test)

# Add sub directories
# This will create the following directories in build
add_subdirectory(project)
add_subdirectory(bin)

